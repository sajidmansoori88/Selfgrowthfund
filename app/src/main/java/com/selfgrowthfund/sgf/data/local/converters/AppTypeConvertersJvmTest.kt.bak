package com.selfgrowthfund.sgf.data.local.converters

import com.selfgrowthfund.sgf.model.enums.*
import com.selfgrowthfund.sgf.data.local.converters.Converters

import org.junit.Test
import org.junit.Assert.assertEquals

class `AppTypeConvertersJvmTest.bak` {

    @Test fun depositStatus_roundTrip() {
        for (status in DepositStatus.values()) {
            val label = AppTypeConverters.fromDepositStatus(status)
            val restored = AppTypeConverters.toDepositStatus(label)
            assertEquals(status, restored)
        }
    }

    // Add other enums similarly...
}